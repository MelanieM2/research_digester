[project]
name = "research-digester"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==2.7.0",
    "python-dotenv==1.2.2",
]

[tool.uv]
add-bounds = "exact"
exclude-newer = "7 days"
